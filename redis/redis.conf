# Network
bind 0.0.0.0
port 6379
protected-mode no

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence — disabled (pure object cache)
save ""
appendonly no

# Logging
loglevel notice

# Performance
tcp-keepalive 300
timeout 0
tcp-backlog 511
